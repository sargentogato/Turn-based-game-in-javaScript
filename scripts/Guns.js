class Gun {
  constructor(idWeapon, name, power, positionY, positionX) {
    // The idWeapon number is the same than the weapon image.
    this.idWeapon = idWeapon;
    this.name = name;
    this.power = power;
    this.y = positionY;
    this.x = positionX;
  }
}
